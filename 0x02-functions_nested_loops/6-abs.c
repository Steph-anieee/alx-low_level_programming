#include "main.h"

/**
 * _abs - computes the absolutr value of an interger
 * @n: thint to check 
 * Return: he absolute value of int
 */
int _abs(int n)
{
	if (n >=0)
	{
		return(n);
	}
	return (-n);
}
